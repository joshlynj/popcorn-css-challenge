var characterFrequency = function (string) {
    let freq = {} ;
    let resultArray = [];
    // for loop to iterate through string input
    for (let i = 0; i < string.length; i++) {
        let char = string.index(i)
        // if statement to see if char already is exists
        if (freq[char]){
            
        }
    }
  };